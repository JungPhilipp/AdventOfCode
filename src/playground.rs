#[cfg(test)]
mod tests {
    /*
    use itertools::Itertools;
        #[test]
        fn test() {
            for i in (0..10).into_iter().window(2) {
                print(i);
            }
        }
    */
}
